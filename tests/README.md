# tests

Pruebas de este repositorio, ejecutadas por `scripts/test.sh` / `make test`:

* Los 10 namespaces mínimos existen y aplican Pod Security Standard `restricted`.
* Existe una `NetworkPolicy` default-deny reutilizable.

Ver también las subcarpetas: [`conformance/`](conformance/), [`deployment/`](deployment/), [`security/`](security/), [`recovery/`](recovery/).
