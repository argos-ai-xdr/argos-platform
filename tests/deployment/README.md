# deployment

Prueba que `argocd/application-sets/platform-baseline.yaml` sincroniza correctamente en `local`/`laboratory` y que Argo CD detecta drift (aplicar un cambio manual y confirmar que se revierte). Corresponde a la Definition of Done de `argos-platform`: "Argo CD detecta drift".
