# recovery

Prueba `cyber-range/reset/reset.sh` (dos resets consecutivos deben producir el mismo estado) y, cuando ARG-025 defina backup/restore de `platform/opensearch/` y `platform/ceph-rgw/`, la restauración desde backup con evidencia. Definition of Done de `argos-platform`: "backup y restore tienen evidencia".
